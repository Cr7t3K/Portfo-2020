{{ form_start(formSkill, {'attr': {'class': 'form-project'}}) }}
<div class="form-input">
    {{ form_row(formSkill.name, {'attr': {'class': 'input-project-form'}}) }}
    {{ form_row(formSkill.image, {'attr': {'class': 'input-project-form'}}) }}
</div>
    <button class="btn button-save">{{ button_label|default('Save') }}</button>
{{ form_end(formSkill) }}
